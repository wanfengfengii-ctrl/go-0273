# 设施草莓组培瓶苗出瓶驯化联合裁定闭环

## 项目目标

建设一个以继代批次和组培瓶封签为主线的 Go HTTP 后端。任务先锁定母株、脱毒谱系、配方摘要、瓶位、阈值、人员及全部资源需求，再依次完成双人继代确认、三联瓶样本封存、资源租约占用、形态覆盖、根系及理化核验、病毒和内生菌复测、异常复判与双人独立复核，最终在可驯化、无菌隔离或已取消之间产生唯一终局。系统采用事务型持久化、逻辑时钟、一次性锁定闸门、资源租约、不可覆盖证据版本链和终局单写屏障；进程重启后可从数据库恢复任务、有效租约、待重试设备调用、盲码状态和裁定结果。实现规模限定为约 20–28 个生产 Go 文件、2000–3000 行有效生产代码，目标约 2500 行，分布于至少 4 个有真实职责的包；公开测试不少于 4 个文件和 12 个测试用例。

## 端到端业务流程

1. 创建并锁定出瓶任务：提交母株编号、脱毒谱系、培养基配方修订摘要、继代批次、组培瓶封签及瓶位、培养架层位、光照窗口、驯化室窗口、抽样盲码、根系测点、玻璃化评分板位、RT-PCR 板孔、内生菌培养孔位、各类阈值、复核人员候选名单和任务代次。锁定事务校验目录快照、唯一性与资源可用性，成功后形成不可修改的任务快照并进入待继代确认。
2. 两名不同且合格的继代确认人员按当前任务代次提交确认。系统禁止其与后续独立复核人员角色重合；两次确认齐备后进入样本封存中。相同操作号和相同内容可重复取得原结果，操作号内容不一致则整笔拒绝。
3. 按瓶位封存三联瓶样本并绑定唯一盲码，盲码在规定状态前不可揭示。封存完整后，以原子事务占用培养架层位、光照窗口、驯化室窗口、RT-PCR 板孔和内生菌培养孔位；有效开放任务之间不得重叠。换架或启动采集必须先比较任务代次和租约版本，再原子替换所需租约。
4. 按锁定瓶位推进形态覆盖矩阵，记录正常苗、玻璃化苗、褐化苗和污染苗整数计数、玻璃化板位及补测标记；每格必须计数守恒且不得重复。随后按锁定测点采集根长、TTC 根系活力、叶绿素 SPAD、培养温度和光照强度，使用定点整数完成校验与派生计算。所有瓶位和测点闭合后进入病原复测中。
5. 通过可脚本化设备适配器取得病毒 Ct 值、内生菌菌落计数及环境数据。拒绝、断连、超时或格式错误仅写入待重试调用记录；重试次数、目标和逻辑时间确定，失败不产生合格证据，也不释放资源或推进状态。有效病原证据进入按盲码和孔位建立的不可覆盖版本链。
6. 当玻璃化比例、褐化、根系活力、病毒、内生菌或三联样本一致性触发异常时，为当前任务代次创建唯一复判轮次，覆盖受影响瓶位、盲码、测点和孔位。复判证据追加版本而不覆盖历史；旧代次迟到结果可审计保存为失效到达记录，但不得改变当前覆盖格、阈值判断或结论。
7. 所有当前代次证据闭合后进入待独立复核。两名互不相同、与继代确认人员不重合且具资格的人员分别复核证据摘要；竞争提交通过事务串行化。若全部锁定阈值满足则允许写入唯一出瓶驯化凭据并进入可驯化，随后可确认已驯化；若存在不可消除病原或污染风险则可裁定无菌隔离。
8. 取消、无菌隔离和出瓶驯化共用终局单写屏障，多个并发请求只有一个成功。终态后普通操作、读数、复判和复核均以稳定错误码拒绝且不改变数据；取消或隔离时按规则关闭有效租约，已生成的历史证据、错误记录和裁定记录继续保留。

## 核心组件与职责

1. 草莓母株与组培规则目录：管理母株编号、固定脱毒谱系、培养基配方修订及摘要、阈值模板、设备量纲、人员资格和角色约束；锁定时生成目录快照，后续目录更新不改变已锁任务。
2. 组培苗出瓶任务聚合：实现十二阶段状态机、任务代次、一次性锁定闸门、双人继代确认、操作号幂等表、独立复核及终局单写屏障，集中维护合法状态迁移。
3. 瓶样本与资源占用账簿：维护组培瓶封签、瓶位、三联样本、抽样盲码及揭示状态，并以带版本和逻辑到期时间的租约管理培养架层位、光照窗口、驯化室窗口和检测孔位；提供事务化占用、换架、续租、释放与重启恢复。
4. 形态根系及理化采集账簿：维护按瓶位和测点展开的覆盖矩阵、形态守恒、补测标记以及根长、TTC 活力、SPAD、温度和光照定点整数证据；负责溢出、符号、长度、除零和派生值校验。
5. 病原复判及终局仲裁器：维护病毒与内生菌证据版本链、可审计设备调用及确定性重试、异常触发规则、复判轮次、旧代次隔离、证据闭合判定、隔离建议和唯一驯化凭据。
6. Go HTTP API：提供 JSON 接口、请求校验、稳定错误码、确定性原因排序和健康检查；组合领域事务与持久层，不在处理器中复制业务规则。

## 领域规则与不变量

1. 状态仅允许按待锁定、待继代确认、样本封存中、资源占用中、形态采集中、根系核验中、病原复测中、待独立复核、可驯化、已驯化、无菌隔离、已取消推进；无菌隔离和已取消可由规定的风险或取消入口抵达，可驯化只能在证据闭合和双重复核后抵达。
2. 锁定是一次性闸门。母株编号必须属于所给脱毒谱系，培养基摘要必须等于目录当前修订摘要；锁定成功后，瓶位、测点、孔位、阈值、人员集合和任务代次均不可原地修改。
3. 开放任务中，继代批次、组培瓶封签、盲码、培养架层位与时间窗口、光照窗口、驯化室窗口、RT-PCR 板孔和内生菌培养孔位分别唯一。所有冲突检查与写入必须位于同一数据库事务，禁止先查后写式竞争。
4. 每个业务写请求携带操作号、任务代次和规范化请求摘要。首次成功或业务拒绝均保存确定结果；相同操作号、相同摘要返回原结果，不同摘要返回 OPERATION_CONTENT_CONFLICT。
5. 拒绝响应包含稳定错误码和结构化原因列表；原因按母株编号、继代批次、组培瓶封签、瓶位、检测孔位依次升序排序，缺失字段以空值参与排序。事务失败时不得遗留部分样本、租约、覆盖格、盲码揭示、复判证据或终局结论。
6. 三联瓶样本必须全部封存后才能占用资源；盲码在病原复测的授权揭盲点之前不可读取明文。盲码重复、提前揭示或瓶位缺失/重复均整笔拒绝。
7. 每个瓶位的正常苗、玻璃化苗、褐化苗和污染苗必须为非负整数，且总和等于锁定苗数；覆盖矩阵恰好包含全部锁定瓶位和测点。补测只能创建新证据版本，不得覆盖原格。
8. 根长、TTC 活力、SPAD、温度、光强、病毒 Ct 和菌落数均以声明小数位的有界整数保存。解析必须检查输入长度、符号、小数位、乘除溢出和除数非零；任何算术错误发生在写入有效证据之前。
9. 设备调用状态包括待调用、失败待重试、成功和永久失败。故障脚本与逻辑时钟决定结果及下一次重试时间；失败调用不得被解释为阴性或合格，且资源租约保持有效直至流程明确终结。
10. 病毒或内生菌证据以任务代次、复判轮次和版本号追加。相同异常在同一任务代次只能存在一个活动复判轮次；复判集合必须完整覆盖仲裁器列出的受影响瓶位、盲码、测点和孔位。
11. 继代确认者必须是两名不同的合格人员；独立复核者也必须是两名不同人员，并与全部继代确认者互斥。资格取自锁定快照，后续目录变化不追溯改变任务。
12. 可驯化凭据包含任务 ID、锁定摘要、证据摘要、两名复核者、逻辑签发时间和唯一序号。可驯化、无菌隔离、已取消及已驯化相关写入通过同一终局版本条件实现单写；终态后的迟到输入仅返回 TERMINAL_STATE，不生成业务证据。

## 数据模型与持久化

1. CatalogMotherPlant：母株编号、品系信息、固定脱毒谱系 ID、启用状态和版本；MediumRevision：配方 ID、修订号、规范化内容摘要、生效逻辑时间；RuleProfile：各形态、根系、理化和病原阈值及定点精度。
2. Personnel：人员 ID、资格集合和启用状态；TaskSnapshot：任务 ID、母株与谱系、配方摘要、继代批次、全部瓶封签及瓶位、资源需求、盲码、测点、板位、阈值、允许人员、任务代次和锁定摘要。
3. AcclimationTask：当前状态、任务代次、状态版本、创建与更新逻辑时间、终局类型；OperationRecord：任务 ID、操作号、请求摘要、稳定结果码和规范化响应，用于跨重启幂等。
4. SubcultureConfirmation：任务、代次、人员、操作号和确认时间；IndependentReview：复核序号、人员、证据摘要、结论；FinalDecision：终局类型、版本、理由、凭据序号及签发时间。
5. BottleSample：封签、瓶位、锁定苗数和三联样本状态；BlindCode：盲码摘要、绑定样本、揭示状态与揭示审计；不得在未授权响应中暴露盲码明文。
6. ResourceLease：资源类型、规范化资源键、任务、任务代次、租约版本、起止逻辑时间及状态；数据库唯一约束保障同一资源键只能存在一个有效租约。
7. MorphologyCell：瓶位、正常/玻璃化/褐化/污染计数、评分板位、补测标记、证据版本；MeasurementCell：瓶位、测点、指标类型、定点整数值、精度、来源和版本。
8. DeviceCall：设备类型、调用目标、故障脚本步号、尝试次数、状态、下一重试逻辑时间、错误码和原始载荷摘要；进程重启后可确定恢复待重试项。
9. PathogenEvidence：盲码、检测类型、RT-PCR 板孔或培养孔位、Ct 或菌落定点值、任务代次、复判轮次和版本；LateArrival：保存旧代次到达的摘要及失效原因，不参与仲裁。
10. RecheckRound：任务代次、轮次、触发原因、受影响瓶位/盲码/测点/孔位集合、状态及证据摘要；AcclimationPermit：唯一凭据序号、任务摘要、复核签名摘要和签发逻辑时间。
11. 持久化采用支持事务和唯一约束的关系数据库，默认可选 SQLite 文件以便本地与测试运行；迁移建立状态版本、操作号、有效租约、封签、盲码和凭据唯一索引。启动恢复在单事务中装载开放任务，重新调度未到期租约与待重试调用，不凭空推进状态。

## 公开接口

1. POST /api/v1/catalog/mothers、/media、/rules、/personnel：写入测试所需目录版本；GET 对应资源可按稳定键读取。生产部署可将目录管理权限与任务操作权限分开，但项目不扩展通用身份系统。
2. POST /api/v1/tasks：创建待锁定任务；POST /api/v1/tasks/{id}/lock：校验并一次性锁定完整快照。所有写接口接受 X-Operation-ID，主体包含 task_generation。
3. POST /api/v1/tasks/{id}/subculture-confirmations；POST /api/v1/tasks/{id}/samples/seal；POST /api/v1/tasks/{id}/resources/acquire、/switch、/renew：完成确认、封存与并发租约操作。
4. POST /api/v1/tasks/{id}/morphology；POST /api/v1/tasks/{id}/measurements/root；POST /api/v1/tasks/{id}/measurements/physicochemical：以批量原子提交方式写入覆盖格，任何单格非法则整批回滚。
5. POST /api/v1/tasks/{id}/pathogen-calls；POST /api/v1/device-calls/{call_id}/retry；POST /api/v1/tasks/{id}/blind-codes/reveal：驱动设备调用、确定性重试和受控揭盲。
6. POST /api/v1/tasks/{id}/rechecks；POST /api/v1/tasks/{id}/rechecks/{round}/evidence：创建唯一复判并原子提交覆盖证据。
7. POST /api/v1/tasks/{id}/reviews；POST /api/v1/tasks/{id}/finalize，finalize 的 action 仅允许 acclimate、isolate 或 cancel；POST /api/v1/tasks/{id}/acclimation-confirmation 将可驯化推进为已驯化。
8. GET /api/v1/tasks/{id} 返回状态和闭合进度；GET /api/v1/tasks/{id}/evidence 返回按固定键排序的版本化证据；GET /api/v1/tasks/{id}/permit 返回唯一凭据；GET /healthz 和 /readyz 提供进程及数据库就绪状态。
9. 错误响应固定为 {code, message, reasons[]}；reason 含 mother_plant_id、subculture_batch、bottle_seal、bottle_position、test_well、field、detail，服务端保证 reasons 的规范排序。HTTP 状态仅表达协议层类别，测试以稳定业务 code 和精确 reasons 为准。

## 失败边界

1. 目录校验、快照写入、唯一占用和状态迁移处于同一事务；母株/谱系错配、陈旧配方、重复封签或资源冲突均回滚整个锁定请求。
2. 样本封存、覆盖矩阵、复判证据和复核均采用批量全有或全无事务；任一瓶位、测点或孔位非法时不保留部分记录。
3. 数据库唯一索引与任务 state_version 条件更新共同处理并发，不依赖进程内互斥锁保证正确性；进程内锁只可用于减少无效竞争。
4. 设备网络或协议故障止于 DeviceCall 边界，只能追加失败尝试和确定性重试计划；领域证据必须由成功且格式合法的调用产生。
5. 算术与格式解析在证据事务写入前完成，统一返回稳定代码 INVALID_FIXED_POINT、ARITHMETIC_OVERFLOW 或 DIVIDE_BY_ZERO，不允许截断、浮点近似或部分派生值。
6. 旧任务代次、旧复判轮次和终态后输入止于版本屏障。旧代次设备结果可作为 LateArrival 审计记录单独提交，但不得触碰有效证据链或任务状态。
7. 数据库提交成功而响应丢失时，客户端凭相同操作号重试并取得原结果；不同内容复用操作号不会启动新事务。
8. 进程崩溃后由数据库恢复任务、租约、调用尝试和证据版本。恢复过程不自动揭盲、不自动生成合格读数、不自动签发凭据；逻辑时钟使公开测试无需依赖墙钟。

## 验收标准

1. 锁定事务完整固定母株编号、脱毒谱系、培养基配方摘要、继代批次、瓶封签及瓶位、培养架层位、光照与驯化窗口、盲码、根系测点、玻璃化板位、两类检测孔位、全部阈值、人员名单和任务代次；错误谱系或陈旧摘要整笔失败。
2. 开放任务的继代批次、瓶封签、盲码、培养架层位、光照窗口、驯化室窗口和检测孔位均唯一；同步屏障触发的并发锁定、换架或启动最多一个成功，失败方无部分租约。
3. 继代确认、样本封存、形态、揭盲、复判和复核均校验当前代次；相同操作号同内容幂等，不同内容稳定报冲突；两名确认者、两名复核者分别互异且两组人员不重合。
4. 形态与根系覆盖矩阵恰好覆盖锁定瓶位和测点，形态计数守恒，补测形成新版本；根长、TTC、SPAD、温度、光强、Ct 和菌落读数使用定点整数并拒绝非法长度、符号、除零与溢出，失败不写有效格。
5. 四类可控设备的拒绝、断连、超时和格式错误只形成可审计待重试调用；逻辑时钟下的对象、次数、退避时间和最终结果完全确定，失败期间资源不提前释放。
6. 阈值越界、病原疑阳、污染或三联样本分歧仅创建一个当前代次复判轮次，并完整覆盖受影响对象；证据版本不可覆盖，旧代次迟到结果不影响当前闭合度及结论。
7. 全部证据闭合且两名合格人员独立复核后才能签发唯一驯化凭据；同步并发的驯化、隔离和取消仅一个形成终局，终态后的普通写入全部以 TERMINAL_STATE 拒绝且数据摘要不变。
8. 仓库包含可编译运行的 Go 后端、go.mod、数据库迁移、重启恢复及适用于 linux/amd64 与 linux/arm64 的 Docker 构建；生产代码至少 20 个 Go 文件、2000–3000 行有效代码且约 2500 行，至少 4 个业务包，公开测试至少 4 个文件和 12 个确定性用例，并预留从同一初始 main 建立 10 条相互独立缺陷支线的清晰边界。

## 确定性测试场景

1. 锁定一个虚构母株任务，断言快照中所有目录版本、资源、瓶位、测点、阈值、人员和任务代次均被固定，目录后续修订不改变快照。
2. 分别提交错误脱毒谱系和陈旧培养基摘要，断言稳定错误码、规范排序原因和零任务推进。
3. 用同步屏障让两个任务竞争同一继代批次、瓶封签和盲码，断言仅一个锁定成功且失败任务没有任何占用。
4. 让两个任务并发竞争培养架层位、光照窗口、驯化室窗口、RT-PCR 板孔和培养孔位，断言数据库唯一约束下无双重有效租约。
5. 使用同一操作号重复相同继代确认，断言响应完全一致；改动确认人员后复用操作号，断言 OPERATION_CONTENT_CONFLICT。
6. 由同一人员尝试两次继代确认，或让继代确认者参与独立复核，断言角色重合拒绝且状态不推进。
7. 提交缺失瓶位、重复瓶位及正常/玻璃化/褐化/污染总数不守恒的形态批次，断言整个覆盖批次回滚。
8. 提交完整形态矩阵并测试玻璃化比例和褐化阈值的等于、低一单位和高一单位边界，断言仲裁结果精确。
9. 对根长、TTC、SPAD、温度、光强、Ct 和菌落定点输入测试最大合法值、过长、负号违规、乘法溢出及除零，断言无失败派生证据残留。
10. 用固定故障脚本依次模拟荧光定量仪拒绝、断连、超时、格式错误后成功，推进逻辑时钟并断言尝试次数、退避时间、调用目标和 Ct 版本链。
11. 对培养箱探头、光照记录器和菌落读板器运行可控失败重试，断言失败不生成合格证据、不释放租约且重启后继续同一重试序列。
12. 尝试在授权状态前揭示盲码并提交重复盲码，断言提前揭示和重复均整笔失败；授权后揭示结果与三联样本绑定保持一致。
13. 制造玻璃化超限、根活力不足、病毒疑阳和三联样本分歧，断言同一代次只创建一个复判轮次且覆盖集合按瓶位、盲码、测点和孔位确定排序。
14. 提交旧复判代次的迟到设备结果，断言其只进入 LateArrival，不覆盖当前病原证据、不改变闭合进度或任务状态。
15. 在缺少一个瓶位形态格、一个根系测点或一个检测孔位证据时尝试复核和签发，断言 EVIDENCE_INCOMPLETE 并列出精确排序缺口。
16. 两名合格人员完成独立复核后，通过同步屏障并发请求驯化、隔离和取消，断言唯一终局、至多一个凭据及其余请求返回确定冲突。
17. 任务进入可驯化后确认已驯化，再提交迟到读数、复判、复核和普通操作，断言全部返回 TERMINAL_STATE 且持久化摘要不变。
18. 在样本封存、资源占用和设备待重试后关闭并重启服务，断言任务状态、租约版本、操作幂等结果、盲码状态和下一重试逻辑时间完整恢复。
19. 构造多项同时失败且输入顺序随机的请求，重复运行并断言 reasons 始终按母株、继代批次、瓶封签、瓶位、检测孔位排序且 JSON 结果一致。
20. 分别针对谱系校验遗漏、配方摘要陈旧判断、租约竞争、幂等摘要比较、计数守恒、定点溢出、设备失败伪合格、复判代次覆盖、人员角色重合和终局双写设计 10 个独立回归测试目标，便于从同一初始 main 建立互不依赖的缺陷支线。

## 组件追踪关系

1. 草莓母株与组培规则目录 → 锁定快照、母株/谱系匹配、配方摘要新鲜度、阈值与人员资格；覆盖验收 1、3、4。
2. 组培苗出瓶任务聚合 → 十二状态、任务代次、操作号幂等、双人确认与复核、终局单写；覆盖验收 2、3、7。
3. 瓶样本与资源占用账簿 → 三联封存、盲码、封签唯一性和五类资源租约并发；覆盖验收 1、2、3、5。
4. 形态根系及理化采集账簿 → 覆盖矩阵、计数守恒、定点整数与派生值安全；覆盖验收 4、6。
5. 病原复判及终局仲裁器 → 设备重试、病原版本链、异常复判、旧代次隔离、证据闭合和凭据生成；覆盖验收 5、6、7。
6. Go HTTP API → 稳定错误码、原因排序、原子批量接口、健康检查和 Docker 可运行入口；覆盖全部验收，尤其验收 8。

## 独特性

本计划聚焦设施草莓脱毒组培瓶苗从无菌培养架到驯化穴盘之间的联合裁定，独特核心是瓶封签与继代批次双闸门、三联盲码样本、按瓶位展开的形态—根系覆盖矩阵、RT-PCR 与内生菌孔位版本证据，以及驯化/隔离/取消共享的单写终局；其作物、实验室证据、资源组合和状态推进均不复用所列既有项目标题或业务对象。
